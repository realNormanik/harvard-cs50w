from django.apps import AppConfig

class AuctionsConfig(AppConfig):
    # Name of the application
    name = "auctions"

    # Optional verbose name for the application
    verbose_name = "Auction Management"